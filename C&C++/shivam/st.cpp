#include "iostream"
#include <string.h>
#include <ctype.h>
#include <stdio.h>
using namespace std;
int main()
{
    char st[30],i,flag;
    cout<<"enter a  string  ";gets(st);
    for(i=0;i<strlen(st);i++)
    flag==20;
    {
        if(st[i]==0)
        flag=10;

    }
    cout<<st[i];
}