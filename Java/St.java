class St{
    public static void main(String args[])
    {
        String s = "             Hello ",name="Ajay", b = "raman";
        s = s.concat(name);
       s =  s.concat(b);

       // int l = s.length();
        //String n = s.toUpperCase();
        //String ll = s.toLowerCase();

        //System.out.println(l);
        //System.out.println(n);
      //  = s.concat(name);
        //System.out.println(s.concat(name));

        //s.compareTo(anotherString)
        
        //s.indexOf(ch)  // start from 0

        //s.replace(oldChar, newChar)
        //s.substring(beginIndex)   //start from 1
        System.out.println(s.indexOf("a"));
        
        //System.out.println(s.trim());
        
    }
}